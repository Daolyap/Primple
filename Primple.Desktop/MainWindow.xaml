<Window x:Class="Primple.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Primple.Desktop"
        mc:Ignorable="d"
        Title="Primple" Height="800" Width="1200"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="CanResizeWithGrip">
    
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1" CornerRadius="0" CaptionHeight="32" ResizeBorderThickness="4"/>
    </WindowChrome.WindowChrome>

    <Border Background="#CC202020" CornerRadius="10" BorderBrush="#33FFFFFF" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center">
                   <Image Source="/Assets/Logo.png" Width="20" Height="20" Margin="0,0,10,0"/>
                   <TextBlock Text="Primple" Foreground="#DDFFFFFF" FontWeight="SemiBold"/>
                </StackPanel>
                <!-- Window Controls would go here -->
            </Grid>

            <!-- Main Content Area -->
            <Grid Grid.Row="1">
                <TextBlock Text="Welcome to Primple" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24" Opacity="0.8"/>
            </Grid>
        </Grid>
    </Border>
</Window>
