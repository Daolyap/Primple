<UserControl x:Class="Primple.Desktop.Views.TemplatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="40">
            <TextBlock Text="Featured Templates" FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
            <TextBlock Text="Quick presets for popular locations and 3D conversions" Foreground="#88FFFFFF" FontSize="16" Margin="0,0,0,40"/>

            <!-- Maps Section -->
            <TextBlock Text="Popular Map Locations" FontSize="20" FontWeight="Bold" Foreground="#CCFFFFFF" Margin="0,0,0,15"/>
            <WrapPanel Margin="0,0,0,30">
                <Button Style="{StaticResource GlassButtonStyle}" Margin="0,0,20,20" Width="200" Height="140" Click="Template_Click" Tag="EiffelTower">
                    <StackPanel>
                        <TextBlock Text="🗼" FontSize="40" HorizontalAlignment="Center"/>
                        <TextBlock Text="Eiffel Tower" FontWeight="Bold" Margin="0,10,0,0"/>
                        <TextBlock Text="Paris, France" FontSize="11" Foreground="#88FFFFFF"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource GlassButtonStyle}" Margin="0,0,20,20" Width="200" Height="140" Click="Template_Click" Tag="TimesSquare">
                    <StackPanel>
                        <TextBlock Text="🗽" FontSize="40" HorizontalAlignment="Center"/>
                        <TextBlock Text="Times Square" FontWeight="Bold" Margin="0,10,0,0"/>
                        <TextBlock Text="New York, USA" FontSize="11" Foreground="#88FFFFFF"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource GlassButtonStyle}" Margin="0,0,20,20" Width="200" Height="140" Click="Template_Click" Tag="Colosseum">
                    <StackPanel>
                        <TextBlock Text="🏟️" FontSize="40" HorizontalAlignment="Center"/>
                        <TextBlock Text="The Colosseum" FontWeight="Bold" Margin="0,10,0,0"/>
                        <TextBlock Text="Rome, Italy" FontSize="11" Foreground="#88FFFFFF"/>
                    </StackPanel>
                </Button>
            </WrapPanel>

            <!-- Image Section -->
            <TextBlock Text="Image-to-3D Examples" FontSize="20" FontWeight="Bold" Foreground="#CCFFFFFF" Margin="0,0,0,15"/>
            <WrapPanel>
                <Button Style="{StaticResource GlassButtonStyle}" Margin="0,0,20,20" Width="200" Height="140" Click="Template_Click" Tag="Logo3D">
                    <StackPanel>
                        <TextBlock Text="🖼️" FontSize="40" HorizontalAlignment="Center"/>
                        <TextBlock Text="Company Logo" FontWeight="Bold" Margin="0,10,0,0"/>
                        <TextBlock Text="Flat to 3D Relief" FontSize="11" Foreground="#88FFFFFF"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource GlassButtonStyle}" Margin="0,0,20,20" Width="200" Height="140" Click="Template_Click" Tag="HeightmapArt">
                    <StackPanel>
                        <TextBlock Text="⛰️" FontSize="40" HorizontalAlignment="Center"/>
                        <TextBlock Text="Terrain Heightmap" FontWeight="Bold" Margin="0,10,0,0"/>
                        <TextBlock Text="Smooth Landscape" FontSize="11" Foreground="#88FFFFFF"/>
                    </StackPanel>
                </Button>
            </WrapPanel>
            
            <TextBlock x:Name="StatusText" Text="" Foreground="#AAFFFFFF" Margin="0,20,0,0"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
