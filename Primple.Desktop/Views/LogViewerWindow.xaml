<Window x:Class="Primple.Desktop.Views.LogViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Debug Logs - Primple" Height="500" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#1a1a1a">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Debug Log Viewer" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            <TextBlock x:Name="LogCountText" Text=" (0 entries)" Foreground="#888888" FontSize="14" VerticalAlignment="Center"/>
        </StackPanel>
        
        <Border Grid.Row="1" Background="#0F0F0F" CornerRadius="8" BorderBrush="#333333" BorderThickness="1">
            <ListBox x:Name="LogListBox" 
                    Background="Transparent" 
                    BorderThickness="0"
                    Foreground="White"
                    FontFamily="Consolas"
                    FontSize="12"
                    VirtualizingStackPanel.IsVirtualizing="True"
                    VirtualizingStackPanel.VirtualizationMode="Recycling"
                    ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="{Binding Converter={StaticResource LogLevelColorConverter}}" TextWrapping="NoWrap"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Export Logs" Width="100" Height="30" Margin="0,0,10,0" Click="ExportLogs_Click"
                   Background="#333333" Foreground="White" BorderThickness="0"/>
            <Button Content="Clear" Width="80" Height="30" Margin="0,0,10,0" Click="ClearLogs_Click"
                   Background="#333333" Foreground="White" BorderThickness="0"/>
            <Button Content="Close" Width="80" Height="30" Click="Close_Click"
                   Background="#0072FF" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
