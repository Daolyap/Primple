<UserControl x:Class="Primple.Desktop.Views.MapsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Configuration Panel -->
        <Border Grid.Column="0" Margin="0,0,10,0" Style="{StaticResource GlassPanelStyle}" Padding="20">
            <StackPanel>
                <TextBlock Text="Map to 3D" FontSize="20" Foreground="White" Margin="0,0,0,20"/>
                
                <TextBlock Text="Location" Foreground="#AAFFFFFF" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <TextBox Height="30" Margin="0,0,0,5" Background="#20FFFFFF" Foreground="White" BorderBrush="#30FFFFFF" VerticalContentAlignment="Center" Padding="5,0"/>
                <Button Content="Search" Style="{StaticResource GlassButtonStyle}" Margin="0,0,0,20"/>
                
                <TextBlock Text="Mode" Foreground="#AAFFFFFF" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <RadioButton Content="2D Map (Flat)" Foreground="White" IsChecked="True" Margin="0,0,0,5"/>
                <RadioButton Content="3D Terrain" Foreground="White" Margin="0,0,0,20"/>
                
                <TextBlock Text="Size (km)" Foreground="#DDFFFFFF" Margin="0,5"/>
                <Slider Maximum="10" Minimum="1" Value="2" Margin="0,0,0,15"/>

                <Button Content="Generate Model" Style="{StaticResource GlassButtonStyle}" Background="{StaticResource AccentColor}" BorderThickness="0" Margin="0,20,0,0"/>
            </StackPanel>
        </Border>

        <!-- Map Preview -->
        <Border Grid.Column="1" Style="{StaticResource GlassPanelStyle}">
            <Grid>
                <TextBlock Text="Map Preview (Requires API)" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#44FFFFFF"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
