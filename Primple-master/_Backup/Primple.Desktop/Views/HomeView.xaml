<UserControl x:Class="Primple.Desktop.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="600">
            <TextBlock Text="Welcome to Primple" Foreground="White" FontSize="32" FontWeight="Light" Margin="0,0,0,20"/>
            
            <TextBlock Text="Start creating by selecting a tool from the sidebar." 
                       Foreground="#AAFFFFFF" FontSize="16" Margin="0,0,0,40"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="New Project" Style="{StaticResource GlassButtonStyle}" Margin="0,0,10,0" Height="50" FontSize="16"/>
                <Button Grid.Column="1" Content="Open Existing" Style="{StaticResource GlassButtonStyle}" Margin="10,0,0,0" Height="50" FontSize="16"/>
            </Grid>
            
            <Border Margin="0,60,0,0" Style="{StaticResource GlassPanelStyle}" Padding="20">
                <StackPanel>
                    <TextBlock Text="Recent Projects" Foreground="#CCFFFFFF" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <TextBlock Text="No recent projects found." Foreground="#66FFFFFF" FontStyle="Italic"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
