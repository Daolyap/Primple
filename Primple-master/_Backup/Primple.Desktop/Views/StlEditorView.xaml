<UserControl x:Class="Primple.Desktop.Views.StlEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:h="http://helix-toolkit.org/wpf"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Margin="0,0,0,10" Padding="10" Background="#10FFFFFF" CornerRadius="8">
            <StackPanel Orientation="Horizontal">
                <Button Content="Import STL" Style="{StaticResource GlassButtonStyle}" Margin="0,0,10,0" Click="ImportButton_Click"/>
                <Button Content="Save" Style="{StaticResource GlassButtonStyle}" Margin="0,0,10,0"/>
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="10,0"/>
                <Button Content="Reset View" Style="{StaticResource GlassButtonStyle}"/>
            </StackPanel>
        </Border>

        <!-- 3D Viewport -->
        <Border Grid.Row="1" Style="{StaticResource GlassPanelStyle}" ClipToBounds="True">
            <h:HelixViewport3D x:Name="viewport" ZoomExtentsWhenLoaded="True" ShowCoordinateSystem="True">
                <h:HelixViewport3D.DefaultCamera>
                    <PerspectiveCamera Position="8,9,7" LookDirection="-1,-1,-1" UpDirection="0,1,0"/>
                </h:HelixViewport3D.DefaultCamera>
                
                <h:DefaultLights/>
                <h:GridLinesVisual3D Width="200" Length="200" MinorDistance="1" MajorDistance="10" Thickness="0.05"/>
                
                <!-- Model Container -->
                <ModelVisual3D x:Name="modelContainer"/>
                
            </h:HelixViewport3D>
        </Border>
    </Grid>
</UserControl>
